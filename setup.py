#!/usr/bin/env python3
"""
TTSKit setup configuration.

This script sets up the TTSKit package for installation via setuptools. Package metadata,
dependencies, and other configurations are defined in pyproject.toml, adhering to modern
Python packaging standards.
"""

from setuptools import setup

if __name__ == "__main__":
    setup()
